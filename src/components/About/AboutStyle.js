import styled from 'styled-components';
import { Btn } from '../../lib/styles/GeneralStyles';

export const AboutWrapper = styled.div`
    margin-bottom: 70px;
`;

export const AboutText = styled.p`
    max-width: 750px;
    margin: 0 auto;
    margin-bottom: 40px;
    text-align: center;
`;

export const AboutButton = styled(Btn)`
    margin: 0 auto;
`;
