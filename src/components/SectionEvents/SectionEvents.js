import React from 'react';
import './SectionEvents.scss';

const SectionEvents = (props) => {
    return <section className="section-events">{props.children}</section>;
};

export default SectionEvents;
